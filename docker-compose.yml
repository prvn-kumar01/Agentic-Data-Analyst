version: "3.8"

services:
  auto-analyst:
    build: .
    container_name: auto-analyst-ai
    ports:
      - "8501:8501"
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
    restart: unless-stopped
