version: "3.8"

services:
  auto-analyst:
    build: .
    container_name: auto-analyst-ai
    ports:
      - "80:80"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
    restart: unless-stopped
